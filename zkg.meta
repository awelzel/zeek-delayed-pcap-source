[package]
script_dir = scripts
build_command = ./configure && cd build && make
test_command = cd testing && btest -c btest.cfg
summary = TODO: A summary of DelayedPcapSource in one line
description = TODO: A more detailed description of DelayedPcapSource.
	It can span multiple lines, with this indentation.

[template]
source = https://github.com/zeek/package-template
version = v2.0.0
zkg_version = 2.13.0-26
features = plugin

[template_vars]
name = DelayedPcapSource
namespace = Zeek

